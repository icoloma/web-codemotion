#!/bin/bash
# Cambia la foto de un ponente
# Parámetros:
# $1: la URL de la que descargarse la foto
# $2: el fichero a sustituir en src/img/talks/orig (full path)

wget $1 > $2
convert $2 -geometry 100x100 $(echo $2 | sed s/orig/processed/ )